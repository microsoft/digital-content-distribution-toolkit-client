package com.msr.bine_sdk.models;

public enum Error {
    NETWORK_ERROR,
    INVALID_PHONE,
    INVALID_OTP,
    EXCEED_ATTEMPT,
    UNKNOWN_ERROR,
    USER_REFERRED,
    INVALID_REFERRAL,
    API_ERROR,
    INIT_FAILURE
}
